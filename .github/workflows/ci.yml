name: CI

on:
  push:
    branches: [main, modern, master]
  pull_request:

jobs:
  build:
    name: Build (${{ matrix.platform }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: linux
            archive: tar.gz
          - platform: macos
            archive: tar.gz
          - platform: windows
            archive: zip

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup osxcross (macOS cross)
        if: matrix.platform == 'macos'
        uses: mbround18/setup-osxcross@v1
        with:
          osx-version: "12.3"

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            build-essential cmake pkg-config \
            libgl1-mesa-dev libglu1-mesa-dev \
            libsdl2-mixer-dev \
            g++-mingw-w64-x86-64 mingw-w64-tools \
            sqlite3 zip

      - name: Verify toolchains
        run: |
          set -euo pipefail
          echo "Matrix platform: ${{ matrix.platform }}"
          if [[ "${{ matrix.platform }}" == "linux" ]]; then
            echo "Local compilers:"
            gcc --version || true
            g++ --version || true
            echo "Cross compilers:"
            command -v x86_64-w64-mingw32-g++ || echo "Mingw cross-compiler missing"
            x86_64-w64-mingw32-g++ --version || true
          elif [[ "${{ matrix.platform }}" == "windows" ]]; then
            echo "Cross compilers:"
            command -v x86_64-w64-mingw32-g++ || echo "Mingw cross-compiler missing"
            x86_64-w64-mingw32-g++ --version || true
          elif [[ "${{ matrix.platform }}" == "macos" ]]; then
            echo "OSXCROSS_TARGET='$OSXCROSS_TARGET'"
            if [[ -n "${OSXCROSS_TARGET:-}" ]]; then
              ls -la "$OSXCROSS_TARGET/bin" || true
              command -v o64-clang || command -v x86_64-apple-darwin-clang++ || echo "osxcross clang missing"
            else
              echo "OSXCROSS_TARGET not set; setup-osxcross may have failed"
              exit 1
            fi
          fi

      - name: Build (${{ matrix.platform }})
        env:
          BV2_PLATFORM: ${{ matrix.platform }}
          BUILD: ${{ github.workspace }}/build-${{ matrix.platform }}
          OSXCROSS_SDK_VERSION: "12.3"
        run: bash ./scripts/ci-build.sh

      - name: Package (${{ matrix.platform }})
        env:
          BUILD: ${{ github.workspace }}/build-${{ matrix.platform }}
          BV2_PLATFORM: ${{ matrix.platform }}
          BV2_ARCHIVE: ${{ matrix.archive }}
          OSXCROSS_SDK_VERSION: "12.3"
        run: bash ./scripts/package-release.sh

      - name: Upload packages
        uses: actions/upload-artifact@v4
        with:
          name: bv2-${{ matrix.platform }}
          path: dist/*
          if-no-files-found: error
